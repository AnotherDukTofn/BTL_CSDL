# Cấu hình kết nối SQL Server
# Copy file này thành .env và sửa lại cho đúng với máy của bạn
DB_SERVER=localhost
DB_DATABASE=SqlPtit
DB_USER=sa
DB_PASSWORD=your_password_here
DB_PORT=1433

# Nếu dùng Windows Authentication thì bật dòng dưới và tắt DB_USER/DB_PASSWORD
# DB_TRUSTED=true

PORT=3000
